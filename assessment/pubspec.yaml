name: winp_flux_assessment
description: WINP Flux take-home assessment starter project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.6.4
  provider: ^6.1.1
  dio: ^5.4.0
  go_router: ^13.2.0
  flutter_widget_from_html_core: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
